# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
- name: Main test block
  block:
    - name: Setup
      ansible.builtin.debug:
        msg: "Setup"

    - name: Test
      ansible.builtin.debug:
        msg: "Test"

    - name: Create Tensorboard
      google.cloud.gcp_vertexai_tensorboard:
        state: present
        display_name: "{{ resource_name }}"
        description: sample description
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _mytb

    - name: Fetch Tensorboard info
      google.cloud.gcp_vertexai_tensorboard_info:
        region: us-central1
        filters:
          - display_name = "{{ resource_name }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _mytb_info

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _mytb.changed | bool
          - _mytb_info.resources | length >= 1

    - name: Update Tensorboard
      google.cloud.gcp_vertexai_tensorboard:
        state: present
        display_name: "{{ resource_name }}"
        description: "Updated description"
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _mytb

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _mytb.changed | bool
          - _mytb.description == "Updated description"

  always:
    - name: Teardown
      ansible.builtin.debug:
        msg: "Teardown"

    - name: Delete Tensorboard
      google.cloud.gcp_vertexai_tensorboard:
        state: absent
        display_name: "{{ resource_name }}"
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
