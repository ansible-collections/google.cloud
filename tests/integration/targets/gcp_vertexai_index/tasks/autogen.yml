# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
- name: Main test block
  block:
    - name: Setup
      ansible.builtin.debug:
        msg: "Setup"

    - name: Create storage bucket
      google.cloud.gcp_storage_bucket:
        name: "{{ resource_name }}"
        state: present
        location: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _mybucket

    - name: Create delta configs
      ansible.builtin.copy:
        dest: "/tmp/{{ resource_name }}-{{ item }}.json"
        mode: "0644"
        content: |
          {"id": "42", "embedding": [0.5, 1.0], "restricts": [{"namespace": "class", "allow": ["cat", "pet"]},{"namespace": "category", "allow": ["feline"]}]}
          {"id": "43", "embedding": [0.6, 1.0], "restricts": [{"namespace": "class", "allow": ["dog", "pet"]},{"namespace": "category", "allow": ["canine"]}]}
      loop: "{{ range(1, 2+1) | list }}"

    - name: Upload delta configs to bucket
      google.cloud.gcp_storage_object:
        action: upload
        bucket: "{{ resource_name }}"
        src: "/tmp/{{ resource_name }}-{{ item }}.json"
        dest: "contents-{{ item }}/data.json"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      loop: "{{ range(1, 2+1) | list }}"

    - name: Create Index
      google.cloud.gcp_vertexai_index:
        state: present
        display_name: "{{ resource_name }}"
        region: us-central1
        metadata:
          contents_delta_uri: "gs://{{ resource_name }}/contents-1"
          config:
            dimensions: 2
            approximate_neighbors_count: 150
            shard_size: SHARD_SIZE_SMALL
            distance_measure_type: DOT_PRODUCT_DISTANCE
            algorithm_config:
              tree_ah_config:
                leaf_node_embedding_count: 500
                leaf_nodes_to_search_percent: 7
        index_update_method: BATCH_UPDATE
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myidx

    - name: Test
      ansible.builtin.debug:
        msg: "Test"

    - name: Fetch index info
      google.cloud.gcp_vertexai_index_info:
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myidx_info

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myidx.changed | bool
          - _myidx_info.resources | length >= 1

    - name: Attempt to update description and metadata.contentsDeltaUri Index
      google.cloud.gcp_vertexai_index:
        state: present
        display_name: "{{ resource_name }}"
        description: "Updated description"
        region: us-central1
        metadata:
          contents_delta_uri: "gs://{{ resource_name }}/contents-2"
          config:
            dimensions: 2
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myidx
      ignore_errors: true

    - name: Previous task should have failed
      ansible.builtin.assert:
        that:
          - _myidx.failed | bool

    - name: Update Index description only
      google.cloud.gcp_vertexai_index:
        state: present
        display_name: "{{ resource_name }}"
        description: "Updated description"
        metadata:
          config:
            dimensions: 2
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myidx

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myidx.changed | bool
          - _myidx.description == "Updated description"

    - name: Update Index metadata only
      google.cloud.gcp_vertexai_index:
        state: present
        display_name: "{{ resource_name }}"
        region: us-central1
        metadata:
          contents_delta_uri: "gs://{{ resource_name }}/contents-2"
          config:
            dimensions: 2
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myidx

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myidx.changed | bool

  always:
    - name: Teardown
      ansible.builtin.debug:
        msg: "Teardown"

    - name: Delete Index
      google.cloud.gcp_vertexai_index:
        state: absent
        display_name: "{{ resource_name }}"
        metadata:
          config:
            dimensions: 2
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Delete delta configs
      ansible.builtin.file:
        path: "/tmp/{{ resource_name }}-{{ item }}.json"
        state: absent
      loop: "{{ range(1, 2+1) | list }}"

    - name: Destroy storage bucket
      block:
        - name: Create temporary directory
          ansible.builtin.tempfile:
            state: directory
          register: _tempdir

        - name: Activate service account
          environment:
            CLOUDSDK_CONFIG: "{{ _tempdir.path }}"
            GOOGLE_APPLICATION_CREDENTIALS: "{{ gcp_cred_file }}"
          ansible.builtin.command: |
            gcloud auth activate-service-account --key-file="$GOOGLE_APPLICATION_CREDENTIALS"

        - name: Remove bucket and contents
          environment:
            CLOUDSDK_CONFIG: "{{ _tempdir.path }}"
            GOOGLE_APPLICATION_CREDENTIALS: "{{ gcp_cred_file }}"
          ansible.builtin.command: |
            gcloud storage rm --recursive gs://{{ resource_name }}/
          failed_when: false
          changed_when: true

      always:
        - name: Delete temporary directory
          ansible.builtin.file:
            path: "{{ _tempdir.path }}"
            state: absent
