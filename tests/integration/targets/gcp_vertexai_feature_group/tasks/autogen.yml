# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
- name: Main test block
  block:
    - name: Setup
      ansible.builtin.debug:
        msg: "Setup"

    - name: Create BigQuery Dataset
      google.cloud.gcp_bigquery_dataset:
        state: present
        name: "{{ resource_name | regex_replace('-', '_') }}"
        dataset_reference:
          dataset_id: "{{ resource_name | regex_replace('-', '_') }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Create BigQuery Table
      google.cloud.gcp_bigquery_table:
        state: present
        name: "{{ resource_name }}_fg"
        dataset: "{{ resource_name | regex_replace('-', '_') }}"
        table_reference:
          dataset_id: "{{ resource_name | regex_replace('-', '_') }}"
          table_id: "{{ resource_name }}_fg"
          project_id: "{{ gcp_project }}"
        schema:
          fields:
            - name: entity_id
              type: STRING
              mode: REQUIRED
            - name: feature_timestamp
              type: TIMESTAMP
            - name: feature_a
              type: FLOAT64
            - name: feature_b
              type: STRING
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Create feature group
      google.cloud.gcp_vertexai_feature_group:
        state: present
        name: "{{ resource_name | regex_replace('-', '_') }}_fg"
        description: "My feature group"
        region: us-central1
        big_query:
          big_query_source:
            input_uri: "bq://{{ gcp_project }}.{{ resource_name | regex_replace('-', '_') }}.{{ resource_name }}_fg"
        labels:
          env: ci
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfg

    - name: Test
      ansible.builtin.debug:
        msg: "Test"

    - name: Fetch feature group info
      google.cloud.gcp_vertexai_feature_group_info:
        region: us-central1
        filters:
          - labels.env = "ci"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfg_info

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myfg.changed | bool
          - _myfg_info.resources | length >= 1

    - name: Update feature group
      google.cloud.gcp_vertexai_feature_group:
        state: present
        name: "{{ resource_name | regex_replace('-', '_') }}_fg"
        description: "Updated description"
        region: us-central1
        big_query:
          big_query_source:
            input_uri: "bq://{{ gcp_project }}.{{ resource_name | regex_replace('-', '_') }}.{{ resource_name }}_fg"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfg

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myfg.changed | bool

  always:
    - name: Teardown
      ansible.builtin.debug:
        msg: "Teardown"

    - name: Delete feature group
      google.cloud.gcp_vertexai_feature_group:
        state: absent
        name: "{{ resource_name | regex_replace('-', '_') }}_fg"
        region: us-central1
        big_query:
          big_query_source:
            input_uri: "bq://{{ gcp_project }}.{{ resource_name | regex_replace('-', '_') }}.{{ resource_name }}_fg"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Delete BigQuery Table
      google.cloud.gcp_bigquery_table:
        state: absent
        name: "{{ resource_name }}_fg"
        dataset: "{{ resource_name | regex_replace('-', '_') }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
