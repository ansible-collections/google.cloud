# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
- name: Main test block
  block:
    - name: Setup
      ansible.builtin.debug:
        msg: "Setup"

    - name: Create Container Analysis Note
      google.cloud.gcp_containeranalysis_note:
        name: "{{ resource_name }}"
        state: present
        attestation_authority:
          hint:
            human_readable_name: "{{ gcp_integration_email }}"
        short_description: "Integration team"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"
      register: _mynote

    - name: Create Basic Attestor
      vars:
        _public_key: |
          
      google.cloud.gcp_binaryauthorization_attestor:
        name: "{{ resource_name }}"
        state: present
        attestation_authority_note:
          note_reference: "{{ _mynote.id }}"
          public_keys:
            - ascii_armored_pgp_public_key: "{{ _public_key | b64decode }}"
        description: "My test attestor"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"
      register: _my_attestor

    - name: Create Custom Policy
      google.cloud.gcp_binaryauthorization_policy:
        state: present
        admission_whitelist_patterns:
          - name_pattern: "gcr.io/google_containers/*"
        default_admission_rule:
          evaluation_mode: ALWAYS_ALLOW
          enforcement_mode: ENFORCED_BLOCK_AND_AUDIT_LOG
        cluster_admission_rules:
          - cluster: us-central1-a.my-cluster
            evaluation_mode: REQUIRE_ATTESTATION
            enforcement_mode: ENFORCED_BLOCK_AND_AUDIT_LOG
            require_attestations_by:
              - "{{ _my_attestor.id }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"
      register: _my_policy

    - name: Test
      ansible.builtin.debug:
        msg: "Test"

    - name: Fetch Custom Policy Info
      google.cloud.gcp_binaryauthorization_policy_info:
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"
      register: _my_policy_info

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _my_policy.changed | bool
          - _my_policy_info.resources | length == 1  # singletons return a single-item list
          - _my_policy_info.resources[0].clusterAdmissionRules is defined
          - _my_policy_info.resources[0].clusterAdmissionRules["us-central1-a.my-cluster"] is defined
          - _my_policy_info.resources[0].clusterAdmissionRules["us-central1-a.my-cluster"].evaluationMode is defined
          - _my_policy_info.resources[0].clusterAdmissionRules["us-central1-a.my-cluster"].evaluationMode == "REQUIRE_ATTESTATION"

  always:
    - name: Teardown
      ansible.builtin.debug:
        msg: "Teardown"

    - name: Delete Custom Policy
      google.cloud.gcp_binaryauthorization_policy:
        state: absent
        default_admission_rule:
          evaluation_mode: ALWAYS_ALLOW
          enforcement_mode: ENFORCED_BLOCK_AND_AUDIT_LOG
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"

    - name: Delete Attestor
      google.cloud.gcp_binaryauthorization_attestor:
        name: "{{ resource_name }}"
        state: absent
        attestation_authority_note:
          note_reference: "{{ _mynote.id }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"

    - name: Delete Container Analysis Note
      google.cloud.gcp_containeranalysis_note:
        name: "{{ resource_name }}"
        state: absent
        attestation_authority:
          hint:
            human_readable_name: "{{ gcp_integration_email }}"
        short_description: "Integration team"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file | default(omit) }}"
