# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
- name: Main test block
  block:
    - name: Setup
      ansible.builtin.debug:
        msg: "Setup"

    - name: Create BigQuery Dataset
      google.cloud.gcp_bigquery_dataset:
        state: present
        name: "{{ resource_name | regex_replace('-', '_') }}"
        dataset_reference:
          dataset_id: "{{ resource_name | regex_replace('-', '_') }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Create BigQuery Table
      google.cloud.gcp_bigquery_table:
        state: present
        name: "{{ resource_name }}_fosf"
        dataset: "{{ resource_name | regex_replace('-', '_') }}"
        table_reference:
          dataset_id: "{{ resource_name | regex_replace('-', '_') }}"
          table_id: "{{ resource_name }}_fosf"
          project_id: "{{ gcp_project }}"
        schema:
          fields:
            - name: entity_id
              type: STRING
              mode: REQUIRED
            - name: feature_timestamp
              type: TIMESTAMP
            - name: feature_a
              type: FLOAT64
            - name: feature_b
              type: STRING
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Create Feature Online Store
      google.cloud.gcp_vertexai_feature_online_store:
        state: present
        name: "{{ resource_name | regex_replace('-', '_') }}_fosf"
        bigtable:
          auto_scaling:
            min_node_count: 1
            max_node_count: 3
            cpu_utilization_target: 50
        region: us-central1
        labels:
          env: ci
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfos

    - name: Test
      ansible.builtin.debug:
        msg: "Test"

    - name: Create Feature Online Store Featureview
      google.cloud.gcp_vertexai_feature_online_store_featureview:
        state: present
        name: "{{ resource_name | regex_replace('-', '_') }}"
        feature_online_store: "{{ resource_name | regex_replace('-', '_') }}_fosf"
        sync_config:
          cron: "0 0 * * *"
        big_query_source:
          uri: "bq://{{ gcp_project }}.{{ resource_name | regex_replace('-', '_') }}.{{ resource_name }}_fosf"
          entity_id_columns:
            - entity_id
        region: us-central1
        labels:
          env: ci
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfosf

    - name: Fetch Feature Online Store Featureview info
      google.cloud.gcp_vertexai_feature_online_store_featureview_info:
        region: us-central1
        feature_online_store: "{{ resource_name | regex_replace('-', '_') }}_fosf"
        filters:
          - labels.env = "ci"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfosf_info

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myfosf.changed | bool
          - _myfosf_info.resources | length >= 1

  always:
    - name: Teardown
      ansible.builtin.debug:
        msg: "Teardown"

    - name: Delete Feature Online Store Featureview
      google.cloud.gcp_vertexai_feature_online_store_featureview:
        state: absent
        name: "{{ resource_name | regex_replace('-', '_') }}"
        feature_online_store: "{{ resource_name | regex_replace('-', '_') }}_fosf"
        sync_config:
          cron: "0 0 * * *"
        big_query_source:
          uri: "bq://{{ gcp_project }}.{{ resource_name | regex_replace('-', '_') }}.{{ resource_name }}_fosf"
          entity_id_columns:
            - entity_id
        region: us-central1
        labels:
          env: ci
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myfosf

    - name: Delete Feature Online Store
      google.cloud.gcp_vertexai_feature_online_store:
        state: absent
        name: "{{ resource_name | regex_replace('-', '_') }}_fosf"
        bigtable:
          auto_scaling:
            min_node_count: 1
            max_node_count: 3
            cpu_utilization_target: 50
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"

    - name: Delete BigQuery Table
      google.cloud.gcp_bigquery_table:
        state: absent
        name: "{{ resource_name }}_fosf"
        dataset: "{{ resource_name | regex_replace('-', '_') }}"
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
