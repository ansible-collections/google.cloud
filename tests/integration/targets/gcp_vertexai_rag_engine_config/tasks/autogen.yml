# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
- name: Main test block
  block:
    - name: Setup
      ansible.builtin.debug:
        msg: "Setup"

    - name: Test
      ansible.builtin.debug:
        msg: "Test"

    - name: Create Basic RAG Engine Config
      google.cloud.gcp_vertexai_rag_engine_config:
        state: present
        rag_managed_config: basic
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myrec

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - _myrec.changed == true

    - name: Noop RAG Engine Config
      google.cloud.gcp_vertexai_rag_engine_config:
        state: present
        rag_managed_config: basic
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
      register: _myrec

    - name: Run assertions
      ansible.builtin.assert:
        that:
          - not (_myrec.changed | bool)

  always:
    - name: Teardown
      ansible.builtin.debug:
        msg: "Teardown"

    - name: Delete RAG Engine Config
      google.cloud.gcp_vertexai_rag_engine_config:
        state: absent
        rag_managed_config: unprovisioned
        region: us-central1
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        service_account_file: "{{ gcp_cred_file }}"
