#!/usr/bin/python
# -*- coding: utf-8 -*-
#
# Copyright (C) 2017-2026 Google
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
#

from __future__ import absolute_import, division, print_function

__metaclass__ = type

################################################################################
# Documentation
################################################################################

ANSIBLE_METADATA = {
    "metadata_version": "1.1",
    "status": ["preview"],
    "supported_by": "community",
}

DOCUMENTATION = r"""
---
author:
  - Google Inc. (@googlecloudplatform)
description:
  - An attestor that attests to container image artifacts.
extends_documentation_fragment:
  - google.cloud.gcp
module: gcp_binaryauthorization_attestor
notes:
  - 'API Reference: U(https://cloud.google.com/binary-authorization/docs/reference/rest/)'
  - 'Official Documentation Guide: U(https://cloud.google.com/binary-authorization/)'
options:
  attestation_authority_note:
    description:
      - A Container Analysis ATTESTATION_AUTHORITY Note, created by the user.
    required: true
    suboptions:
      delegation_service_account_email:
        description:
          - This field will contain the service account email address that this Attestor will use as the principal when querying Container Analysis.
          - Attestor administrators must grant this service account the IAM role needed to read attestations from the noteReference in Container Analysis (containeranalysis.notes.occurrences.viewer).
          - This email address is fixed for the lifetime of the Attestor, but callers should not make any other assumptions about the service account email; future versions may use an email based on a different naming pattern.
        type: str
      note_reference:
        description:
          - The resource name of a ATTESTATION_AUTHORITY Note, created by the user.
          - If the Note is in a different project from the Attestor, it should be specified in the format `projects/*/notes/*` (or the legacy `providers/*/notes/*`).
          - This field may not be updated.
          - An attestation by this attestor is stored as a Container Analysis ATTESTATION_AUTHORITY Occurrence that names a container image and that links to this Note.
          - This property is immutable, to change it, you must delete and recreate the resource.
        required: true
        type: str
      public_keys:
        description:
          - Public keys that verify attestations signed by this attestor.
          - This field may be updated.
          - If this field is non-empty, one of the specified public keys must verify that an attestation was signed by this attestor for the image specified in the admission request.
          - If this field is empty, this attestor always returns that no valid attestations exist.
        elements: dict
        suboptions:
          ascii_armored_pgp_public_key:
            description:
              - ASCII-armored representation of a PGP public key, as the entire output by the command `gpg --export --armor foo@example.com` (either LF or CRLF line endings).
              - When using this field, id should be left blank.
              - The BinAuthz API handlers will calculate the ID and fill it in automatically.
              - BinAuthz computes this ID as the OpenPGP RFC4880 V4 fingerprint, represented as upper-case hex.
              - If id is provided by the caller, it will be overwritten by the API-calculated ID.
            type: str
          comment:
            description:
              - A descriptive comment.
              - This field may be updated.
            type: str
          id:
            description:
              - The ID of this public key.
              - Signatures verified by BinAuthz must include the ID of the public key that can be used to verify them, and that ID must match the contents of this field exactly.
              - Additional restrictions on this field can be imposed based on which public key type is encapsulated.
              - See the documentation on publicKey cases below for details.
            type: str
          pkix_public_key:
            description:
              - A raw PKIX SubjectPublicKeyInfo format public key.
              - 'NOTE: id may be explicitly provided by the caller when using this type of public key, but it MUST be a valid RFC3986 URI.'
              - If id is left blank, a default one will be computed based on the digest of the DER encoding of the public key.
            suboptions:
              public_key_pem:
                description:
                  - A PEM-encoded public key, as described in `https://tools.ietf.org/html/rfc7468#section-13`.
                type: str
              signature_algorithm:
                description:
                  - The signature algorithm used to verify a message against a signature using this key.
                  - These signature algorithm must match the structure and any object identifiers encoded in publicKeyPem (i.e.
                  - this algorithm must match that of the public key).
                type: str
            type: dict
        type: list
    type: dict
  description:
    description:
      - A descriptive comment.
      - This field may be updated.
      - The field may be displayed in chooser dialogs.
    type: str
  name:
    description:
      - The resource name.
      - This property is immutable, to change it, you must delete and recreate the resource.
    required: true
    type: str
  state:
    choices:
      - present
      - absent
    default: present
    description:
      - Whether the resource should exist.
    type: str
requirements:
  - python >= 3.8
  - requests >= 2.18.4
  - google-auth >= 2.25.1
short_description: Manages a BinaryAuthorization.Attestor resource
"""  # noqa: E501

EXAMPLES = r"""
- name: Read armored public key file
  ansible.builtin.slurp:
    path: /path/to/public-key.asc
  register: _public_key

- name: Create Attestor
  google.cloud.gcp_binaryauthorization_attestor:
    name: test-attestor
    state: present
    attestation_authority_note:
      note_reference: projects/my-project/notes/my-note
      public_keys:
        - ascii_armored_pgp_public_key: "{{ _public_key.content | b64decode }}"

################################################################################

- name: Delete Attestor
  google.cloud.gcp_binaryauthorization_attestor:
    name: test-attestor
    state: absent
    attestation_authority_note:
      note_reference: projects/my-project/notes/my-note
"""  # noqa: E501

RETURN = r"""
changed:
  description: Whether the resource was changed.
  returned: always
  type: bool
description:
  description:
    - A descriptive comment.
    - This field may be updated.
    - The field may be displayed in chooser dialogs.
  returned: when set
  type: str
name:
  description:
    - The resource name.
    - This property is immutable, to change it, you must delete and recreate the resource.
  returned: always
  type: str
state:
  description: The current state of the resource.
  returned: always
  type: str
userOwnedGrafeasNote:
  contains:
    delegationServiceAccountEmail:
      description:
        - This field will contain the service account email address that this Attestor will use as the principal when querying Container Analysis.
        - Attestor administrators must grant this service account the IAM role needed to read attestations from the noteReference in Container Analysis (containeranalysis.notes.occurrences.viewer).
        - This email address is fixed for the lifetime of the Attestor, but callers should not make any other assumptions about the service account email; future versions may use an email based on a different naming pattern.
      returned: success
      type: str
    noteReference:
      description:
        - The resource name of a ATTESTATION_AUTHORITY Note, created by the user.
        - If the Note is in a different project from the Attestor, it should be specified in the format `projects/*/notes/*` (or the legacy `providers/*/notes/*`).
        - This field may not be updated.
        - An attestation by this attestor is stored as a Container Analysis ATTESTATION_AUTHORITY Occurrence that names a container image and that links to this Note.
        - This property is immutable, to change it, you must delete and recreate the resource.
      returned: always
      type: str
    publicKeys:
      contains:
        asciiArmoredPgpPublicKey:
          description:
            - ASCII-armored representation of a PGP public key, as the entire output by the command `gpg --export --armor foo@example.com` (either LF or CRLF line endings).
            - When using this field, id should be left blank.
            - The BinAuthz API handlers will calculate the ID and fill it in automatically.
            - BinAuthz computes this ID as the OpenPGP RFC4880 V4 fingerprint, represented as upper-case hex.
            - If id is provided by the caller, it will be overwritten by the API-calculated ID.
          returned: when set
          type: str
        comment:
          description:
            - A descriptive comment.
            - This field may be updated.
          returned: when set
          type: str
        id:
          description:
            - The ID of this public key.
            - Signatures verified by BinAuthz must include the ID of the public key that can be used to verify them, and that ID must match the contents of this field exactly.
            - Additional restrictions on this field can be imposed based on which public key type is encapsulated.
            - See the documentation on publicKey cases below for details.
          returned: when set
          type: str
        pkixPublicKey:
          contains:
            publicKeyPem:
              description:
                - A PEM-encoded public key, as described in `https://tools.ietf.org/html/rfc7468#section-13`.
              returned: when set
              type: str
            signatureAlgorithm:
              description:
                - The signature algorithm used to verify a message against a signature using this key.
                - These signature algorithm must match the structure and any object identifiers encoded in publicKeyPem (i.e.
                - this algorithm must match that of the public key).
              returned: when set
              type: str
          description:
            - A raw PKIX SubjectPublicKeyInfo format public key.
            - 'NOTE: id may be explicitly provided by the caller when using this type of public key, but it MUST be a valid RFC3986 URI.'
            - If id is left blank, a default one will be computed based on the digest of the DER encoding of the public key.
          returned: when set
          type: dict
      description:
        - Public keys that verify attestations signed by this attestor.
        - This field may be updated.
        - If this field is non-empty, one of the specified public keys must verify that an attestation was signed by this attestor for the image specified in the admission request.
        - If this field is empty, this attestor always returns that no valid attestations exist.
      elements: dict
      returned: when set
      type: list
  description:
    - A Container Analysis ATTESTATION_AUTHORITY Note, created by the user.
  returned: always
  type: dict
"""  # noqa: E501

################################################################################
# Imports
################################################################################

from ansible_collections.google.cloud.plugins.module_utils import gcp_v2

# BEGIN Custom imports
# END Custom imports


class AttestationAuthorityNote(gcp_v2.Resource):
    def _request(self):
        return {
            "noteReference": self.request.get("note_reference"),
            "publicKeys": [
                AttestationAuthorityNotePublicKey(item).to_request() for item in (self.request.get("public_keys") or [])
            ],
        }

    def _response(self):
        return {
            "delegationServiceAccountEmail": self.response.get("delegationServiceAccountEmail"),
        }


class AttestationAuthorityNotePublicKey(gcp_v2.Resource):
    def _request(self):
        return {
            "asciiArmoredPgpPublicKey": self.request.get("ascii_armored_pgp_public_key"),
            "comment": self.request.get("comment"),
            "id": self.request.get("id"),
            "pkixPublicKey": gcp_v2.remove_empties(
                AttestationAuthorityNotePublicKeyPkixPublicKey(self.request.get("pkix_public_key", {})).to_request()
            ),  # remove empty values
        }


class AttestationAuthorityNotePublicKeyPkixPublicKey(gcp_v2.Resource):
    def _request(self):
        return {
            "publicKeyPem": self.request.get("public_key_pem"),
            "signatureAlgorithm": self.request.get("signature_algorithm"),
        }


class BinaryAuthorization(gcp_v2.Resource):
    def _request(self):
        return {
            "userOwnedGrafeasNote": gcp_v2.remove_empties(
                AttestationAuthorityNote(self.request.get("attestation_authority_note", {})).to_request()
            ),  # remove empty values
            "description": self.request.get("description"),
            "name": self.request.get("name"),
        }

    def _response(self):
        return {}

    def decode(self, response):
        "Custom decoder function, mutates the response object before it is returned to the module caller."

        # --------- BEGIN custom decoder code ---------
        # flatten name
        self.debug(response=response, decoded=False)
        if not gcp_v2.empty(response):
            if response.get("name"):
                n: str = response.pop("name")
                response["id"] = n
                response["name"] = gcp_v2.flatten_name(n)
        self.debug(response=response, decoded=True)

        return response

        # --------- END custom decoder code ---------


################################################################################
# Main
################################################################################


def main():
    """Main function"""

    module = gcp_v2.Module(
        argument_spec=dict(
            name=dict(
                type="str",
                required=True,
            ),
            state=dict(
                type="str",
                default="present",
                choices=["present", "absent"],
            ),
            attestation_authority_note=dict(
                type="dict",
                required=True,
                no_log=False,
                options=dict(
                    delegation_service_account_email=dict(
                        type="str",
                    ),
                    note_reference=dict(
                        type="str",
                        required=True,
                    ),
                    public_keys=dict(
                        type="list",
                        elements="dict",
                        no_log=False,
                        options=dict(
                            ascii_armored_pgp_public_key=dict(
                                type="str",
                                no_log=False,
                            ),
                            comment=dict(
                                type="str",
                            ),
                            id=dict(
                                type="str",
                            ),
                            pkix_public_key=dict(
                                type="dict",
                                no_log=False,
                                options=dict(
                                    public_key_pem=dict(
                                        type="str",
                                        no_log=False,
                                    ),
                                    signature_algorithm=dict(
                                        type="str",
                                    ),
                                ),
                            ),
                        ),
                    ),
                ),
            ),
            description=dict(
                type="str",
            ),
        )
    )

    if not module.params["scopes"]:
        module.params["scopes"] = ["https://www.googleapis.com/auth/cloud-platform"]

    state = module.params["state"]
    changed = False
    op_configs = gcp_v2.ResourceOpConfigs(
        base_url="https://binaryauthorization.googleapis.com/v1/",
        base_uri="projects/{project}/attestors",
        configs={
            "create": gcp_v2.ResourceOpConfig(
                **{
                    "uri": "projects/{project}/attestors?attestorId={name}",
                    "async_uri": "",
                    "verb": "POST",
                    "timeout_minutes": 20,
                }
            ),
            "delete": gcp_v2.ResourceOpConfig(
                **{
                    "uri": "projects/{project}/attestors/{name}",
                    "async_uri": "",
                    "verb": "DELETE",
                    "timeout_minutes": 20,
                }
            ),
            "read": gcp_v2.ResourceOpConfig(
                **{"uri": "projects/{project}/attestors/{name}", "async_uri": "", "verb": "GET", "timeout_minutes": 0}
            ),
            "update": gcp_v2.ResourceOpConfig(
                **{"uri": "projects/{project}/attestors/{name}", "async_uri": "", "verb": "PUT", "timeout_minutes": 20}
            ),
        },
    )

    request = gcp_v2.remove_nones(module.params)
    resource = BinaryAuthorization(
        request,
        module=module,
        product="BinaryAuthorization",
        kind="binaryauthorization#attestor",
        op_configs=op_configs,
    )

    resource._state = state  # store the state in the resource object

    # Set this variable in one of the pre steps to implement custom diff logic
    custom_diff = None

    # BEGIN massaging ResourceRef properties
    # END massaging ResourceRef properties

    read_link: str = ""  # give it a chance for pre-read to overload

    if read_link == "":
        read_link = resource.build_link("read")
    existing_obj = resource.from_response(resource.get(read_link, allow_not_found=True) or {})
    new_obj = {}
    gcp_v2.debug(module, request=gcp_v2.remove_empties(resource.to_request()), existing=existing_obj, post=False)

    if custom_diff is not None:
        is_different = custom_diff
    else:
        is_different = resource.diff(gcp_v2.remove_empties(existing_obj))

    gcp_v2.debug(
        module,
        request=gcp_v2.remove_empties(resource.to_request()),
        existing=existing_obj,
        post=True,
        is_different=is_different,
    )

    if gcp_v2.empty(existing_obj):
        if state == "present":
            gcp_v2.debug(module, action="create")
            try:
                # --------- BEGIN create code ---------
                create_link: str = ""  # give it a chance for pre-create to overload
                if create_link == "":
                    create_link = resource.build_link("create")
                create_retries = op_configs.create.timeout
                create_func = getattr(resource, op_configs.create.verb)
                create_async_uri = op_configs.create.async_uri
                create_async_func = getattr(resource, op_configs.create.verb + "_async")
                gcp_v2.debug(module, msg="Creating resource", create_link=create_link, async_uri=create_async_uri)

                if create_async_uri != "":
                    new_obj = create_async_func(create_link, async_uri=create_async_uri, retries=create_retries)
                else:
                    new_obj = create_func(create_link)
                new_obj = resource.with_kind(resource.from_response(new_obj))
                gcp_v2.debug(module, new=new_obj, action="create", post=False)
                gcp_v2.debug(module, new=new_obj, action="create", post=True)
                # --------- END create code ---------
            except Exception as e:
                module.fail_json(msg=str(e))

            changed = True
        else:
            pass  # nothing to do
    else:
        if state == "absent":
            gcp_v2.debug(module, action="delete")
            try:
                # --------- BEGIN delete code ---------
                delete_link: str = ""  # give it a chance for pre-delete to overload
                if delete_link == "":
                    delete_link = resource.build_link("delete")
                delete_retries = op_configs.delete.timeout
                delete_func = getattr(resource, op_configs.delete.verb)
                delete_async_uri = op_configs.delete.async_uri
                delete_async_func = getattr(resource, op_configs.delete.verb + "_async")
                gcp_v2.debug(
                    module,
                    msg="Destroying resource",
                    delete_link=delete_link,
                    async_uri=delete_async_uri,
                )

                if delete_async_uri != "":
                    new_obj = delete_async_func(delete_link, async_uri=delete_async_uri, retries=delete_retries)
                else:
                    new_obj = delete_func(delete_link)
                new_obj = resource.from_response(new_obj)
                gcp_v2.debug(module, new=new_obj, action="delete", post=False)
                gcp_v2.debug(module, new=new_obj, action="delete", post=True)
                # --------- END delete code ---------
            except Exception as e:
                module.fail_json(msg=str(e))

            changed = True
        else:
            if is_different:
                gcp_v2.debug(module, action="update")
                try:
                    # --------- BEGIN update code ---------
                    update_link: str = ""  # give it a chance for pre-update to overload
                    if update_link == "":
                        update_link = resource.build_link("update")
                    update_retries = op_configs.update.timeout
                    update_func = getattr(resource, op_configs.update.verb)
                    update_async_uri = op_configs.update.async_uri
                    update_async_func = getattr(resource, op_configs.update.verb + "_async")
                    gcp_v2.debug(
                        module,
                        msg="Updating resource",
                        update_link=update_link,
                        async_uri=update_async_uri,
                    )
                    if update_async_uri != "":
                        new_obj = update_async_func(update_link, async_uri=update_async_uri, retries=update_retries)
                    else:
                        new_obj = update_func(update_link)
                    new_obj = resource.with_kind(resource.from_response(new_obj))
                    gcp_v2.debug(module, new=new_obj, action="update", post=False)
                    gcp_v2.debug(module, new=new_obj, action="update", post=True)
                    # --------- END update code ---------
                except Exception as e:
                    module.fail_json(msg=str(e))

                changed = True
            else:
                new_obj = existing_obj

    new_obj = gcp_v2.filter_reserved_keys(new_obj)
    new_obj.update({"changed": changed})
    gcp_v2.debug(module, final_obj=new_obj, changed=changed)
    module.exit_json(**new_obj)


if __name__ == "__main__":
    main()
