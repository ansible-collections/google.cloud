#!/usr/bin/python
# -*- coding: utf-8 -*-
#
# Copyright (C) 2017-2026 Google
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
#

from __future__ import absolute_import, division, print_function

__metaclass__ = type

################################################################################
# Documentation
################################################################################

ANSIBLE_METADATA = {
    "metadata_version": "1.1",
    "status": ["preview"],
    "supported_by": "community",
}

DOCUMENTATION = r"""
---
author:
  - Google Inc. (@googlecloudplatform)
description:
  - A Container Analysis note is a high-level piece of metadata that
  - describes a type of analysis that can be done for a resource.
extends_documentation_fragment:
  - google.cloud.gcp
module: gcp_containeranalysis_note
notes:
  - 'API Reference: U(https://cloud.google.com/container-analysis/api/reference/rest/)'
  - 'Creating Attestations (Occurrences) Guide: U(https://cloud.google.com/binary-authorization/docs/making-attestations)'
  - 'Official Documentation Guide: U(https://cloud.google.com/container-analysis/)'
options:
  attestation_authority:
    description:
      - Note kind that represents a logical attestation "role" or "authority".
      - For example, an organization might have one AttestationAuthority for "QA" and one for "build".
      - This Note is intended to act strictly as a grouping mechanism for the attached Occurrences (Attestations).
      - This grouping mechanism also provides a security boundary, since IAM ACLs gate the ability for a principle to attach an Occurrence to a given Note.
      - It also provides a single point of lookup to find all attached Attestation Occurrences, even if they don't all live in the same project.
    required: true
    suboptions:
      hint:
        description:
          - This submessage provides human-readable hints about the purpose of the AttestationAuthority.
          - Because the name of a Note acts as its resource reference, it is important to disambiguate the canonical name of the Note (which might be a UUID for security purposes) from "readable" names more suitable for debug output.
          - Note that these hints should NOT be used to look up AttestationAuthorities in security sensitive contexts, such as when looking up Attestations to verify.
        required: true
        suboptions:
          human_readable_name:
            description:
              - The human readable name of this Attestation Authority, for example "qa".
            required: true
            type: str
        type: dict
    type: dict
  expiration_time:
    description:
      - Time of expiration for this note.
      - Leave empty if note does not expire.
    type: str
  long_description:
    description:
      - A detailed description of the note.
    type: str
  name:
    description:
      - The name of the note.
      - This property is immutable, to change it, you must delete and recreate the resource.
    required: true
    type: str
  related_note_names:
    description:
      - Names of other notes related to this note.
    elements: str
    type: list
  related_url:
    description:
      - URLs associated with this note and related metadata.
    elements: dict
    suboptions:
      label:
        description:
          - Label to describe usage of the URL.
        type: str
      url:
        description:
          - Specific URL associated with the resource.
        required: true
        type: str
    type: list
  short_description:
    description:
      - A one sentence description of the note.
    type: str
  state:
    choices:
      - present
      - absent
    default: present
    description:
      - Whether the resource should exist.
    type: str
requirements:
  - python >= 3.8
  - requests >= 2.18.4
  - google-auth >= 2.25.1
short_description: Manages a ContainerAnalysis.Note resource
"""  # noqa: E501

EXAMPLES = r"""
- name: Create Container Analysis Note
  google.cloud.gcp_containeranalysis_note:
    name: my-note
    state: present
    attestation_authority:
      hint:
        human_readable_name: "qa@example.com"
    short_description: "QA team attestation"

################################################################################

- name: Delete Container Analysis Note
  google.cloud.gcp_containeranalysis_note:
    name: my-note
    state: absent
    attestation_authority:
      hint:
        human_readable_name: "qa@example.com"
"""  # noqa: E501

RETURN = r"""
attestationAuthority:
  contains:
    hint:
      contains:
        humanReadableName:
          description:
            - The human readable name of this Attestation Authority, for example "qa".
          returned: always
          type: str
      description:
        - This submessage provides human-readable hints about the purpose of the AttestationAuthority.
        - Because the name of a Note acts as its resource reference, it is important to disambiguate the canonical name of the Note (which might be a UUID for security purposes) from "readable" names more suitable for debug output.
        - Note that these hints should NOT be used to look up AttestationAuthorities in security sensitive contexts, such as when looking up Attestations to verify.
      returned: always
      type: dict
  description:
    - Note kind that represents a logical attestation "role" or "authority".
    - For example, an organization might have one AttestationAuthority for "QA" and one for "build".
    - This Note is intended to act strictly as a grouping mechanism for the attached Occurrences (Attestations).
    - This grouping mechanism also provides a security boundary, since IAM ACLs gate the ability for a principle to attach an Occurrence to a given Note.
    - It also provides a single point of lookup to find all attached Attestation Occurrences, even if they don't all live in the same project.
  returned: always
  type: dict
changed:
  description: Whether the resource was changed.
  returned: always
  type: bool
createTime:
  description:
    - The time this note was created.
  returned: success
  type: str
expirationTime:
  description:
    - Time of expiration for this note.
    - Leave empty if note does not expire.
  returned: when set
  type: str
kind:
  description:
    - The type of analysis this note describes.
  returned: success
  type: str
longDescription:
  description:
    - A detailed description of the note.
  returned: when set
  type: str
name:
  description:
    - The name of the note.
    - This property is immutable, to change it, you must delete and recreate the resource.
  returned: always
  type: str
relatedNoteNames:
  description:
    - Names of other notes related to this note.
  elements: str
  returned: when set
  type: list
relatedUrl:
  contains:
    label:
      description:
        - Label to describe usage of the URL.
      returned: when set
      type: str
    url:
      description:
        - Specific URL associated with the resource.
      returned: always
      type: str
  description:
    - URLs associated with this note and related metadata.
  elements: dict
  returned: when set
  type: list
shortDescription:
  description:
    - A one sentence description of the note.
  returned: when set
  type: str
state:
  description: The current state of the resource.
  returned: always
  type: str
updateTime:
  description:
    - The time this note was last updated.
  returned: success
  type: str
"""  # noqa: E501

################################################################################
# Imports
################################################################################

from ansible_collections.google.cloud.plugins.module_utils import gcp_v2

# BEGIN Custom imports
# END Custom imports


class AttestationAuthority(gcp_v2.Resource):
    def _request(self):
        return {
            "hint": gcp_v2.remove_empties(
                AttestationAuthorityHint(self.request.get("hint", {})).to_request()
            ),  # remove empty values
        }


class AttestationAuthorityHint(gcp_v2.Resource):
    def _request(self):
        return {
            "humanReadableName": self.request.get("human_readable_name"),
        }


class RelatedUrl(gcp_v2.Resource):
    def _request(self):
        return {
            "label": self.request.get("label"),
            "url": self.request.get("url"),
        }


class ContainerAnalysis(gcp_v2.Resource):
    def _request(self):
        return {
            "attestationAuthority": gcp_v2.remove_empties(
                AttestationAuthority(self.request.get("attestation_authority", {})).to_request()
            ),  # remove empty values
            "expirationTime": self.request.get("expiration_time"),
            "longDescription": self.request.get("long_description"),
            "name": self.request.get("name"),
            "relatedNoteNames": [str(item) for item in (self.request.get("related_note_names") or [])],
            "relatedUrl": [RelatedUrl(item).to_request() for item in (self.request.get("related_url") or [])],
            "shortDescription": self.request.get("short_description"),
        }

    def _response(self):
        return {
            "createTime": self.response.get("createTime"),
            "kind": self.response.get("kind"),
            "updateTime": self.response.get("updateTime"),
        }

    def encode(self, request):
        "Custom encoder function, mutates the request object before it is sent to the API."

        # --------- BEGIN custom encoder code ---------
        # field was renamed in GA API
        self.debug(request=request, encoded=False)
        if request.get("attestationAuthority"):
            request["attestation"] = request.pop("attestationAuthority")
        self.debug(request=request, encoded=True)

        return request

        # --------- END custom encoder code ---------

    def decode(self, response):
        "Custom decoder function, mutates the response object before it is returned to the module caller."

        # --------- BEGIN custom decoder code ---------
        self.debug(response=response, decoded=False)
        if not gcp_v2.empty(response):
            if response.get("attestation"):
                response["attestationAuthority"] = response.pop("attestation")
            if response.get("name"):
                n: str = response.pop("name")
                response["id"] = n
                response["name"] = gcp_v2.flatten_name(n)
        self.debug(response=response, decoded=True)

        return response

        # --------- END custom decoder code ---------


################################################################################
# Main
################################################################################


def main():
    """Main function"""

    module = gcp_v2.Module(
        argument_spec=dict(
            name=dict(
                type="str",
                required=True,
            ),
            state=dict(
                type="str",
                default="present",
                choices=["present", "absent"],
            ),
            attestation_authority=dict(
                type="dict",
                required=True,
                no_log=False,
                options=dict(
                    hint=dict(
                        type="dict",
                        required=True,
                        options=dict(
                            human_readable_name=dict(
                                type="str",
                                required=True,
                            )
                        ),
                    )
                ),
            ),
            expiration_time=dict(
                type="str",
            ),
            long_description=dict(
                type="str",
            ),
            related_note_names=dict(
                type="list",
                elements="str",
            ),
            related_url=dict(
                type="list",
                elements="dict",
                options=dict(
                    label=dict(
                        type="str",
                    ),
                    url=dict(
                        type="str",
                        required=True,
                    ),
                ),
            ),
            short_description=dict(
                type="str",
            ),
        )
    )

    if not module.params["scopes"]:
        module.params["scopes"] = ["https://www.googleapis.com/auth/cloud-platform"]

    state = module.params["state"]
    changed = False
    op_configs = gcp_v2.ResourceOpConfigs(
        base_url="https://containeranalysis.googleapis.com/v1/",
        base_uri="projects/{project}/notes",
        configs={
            "create": gcp_v2.ResourceOpConfig(
                **{
                    "uri": "projects/{project}/notes?noteId={name}",
                    "async_uri": "",
                    "verb": "POST",
                    "timeout_minutes": 20,
                }
            ),
            "delete": gcp_v2.ResourceOpConfig(
                **{"uri": "projects/{project}/notes/{name}", "async_uri": "", "verb": "DELETE", "timeout_minutes": 20}
            ),
            "read": gcp_v2.ResourceOpConfig(
                **{"uri": "projects/{project}/notes/{name}", "async_uri": "", "verb": "GET", "timeout_minutes": 0}
            ),
            "update": gcp_v2.ResourceOpConfig(
                **{"uri": "projects/{project}/notes/{name}", "async_uri": "", "verb": "PATCH", "timeout_minutes": 20}
            ),
        },
    )

    request = gcp_v2.remove_nones(module.params)
    resource = ContainerAnalysis(
        request, module=module, product="ContainerAnalysis", kind="containeranalysis#note", op_configs=op_configs
    )

    resource._state = state  # store the state in the resource object

    # Set this variable in one of the pre steps to implement custom diff logic
    custom_diff = None

    # BEGIN massaging ResourceRef properties
    # END massaging ResourceRef properties

    read_link: str = ""  # give it a chance for pre-read to overload

    if read_link == "":
        read_link = resource.build_link("read")
    existing_obj = resource.from_response(resource.get(read_link, allow_not_found=True) or {})
    new_obj = {}
    gcp_v2.debug(module, request=gcp_v2.remove_empties(resource.to_request()), existing=existing_obj, post=False)

    if custom_diff is not None:
        is_different = custom_diff
    else:
        is_different = resource.diff(gcp_v2.remove_empties(existing_obj))

    gcp_v2.debug(
        module,
        request=gcp_v2.remove_empties(resource.to_request()),
        existing=existing_obj,
        post=True,
        is_different=is_different,
    )

    if gcp_v2.empty(existing_obj):
        if state == "present":
            gcp_v2.debug(module, action="create")
            try:
                # --------- BEGIN create code ---------
                create_link: str = ""  # give it a chance for pre-create to overload
                if create_link == "":
                    create_link = resource.build_link("create")
                create_retries = op_configs.create.timeout
                create_func = getattr(resource, op_configs.create.verb)
                create_async_uri = op_configs.create.async_uri
                create_async_func = getattr(resource, op_configs.create.verb + "_async")
                gcp_v2.debug(module, msg="Creating resource", create_link=create_link, async_uri=create_async_uri)

                if create_async_uri != "":
                    new_obj = create_async_func(create_link, async_uri=create_async_uri, retries=create_retries)
                else:
                    new_obj = create_func(create_link)
                new_obj = resource.with_kind(resource.from_response(new_obj))
                gcp_v2.debug(module, new=new_obj, action="create", post=False)
                gcp_v2.debug(module, new=new_obj, action="create", post=True)
                # --------- END create code ---------
            except Exception as e:
                module.fail_json(msg=str(e))

            changed = True
        else:
            pass  # nothing to do
    else:
        if state == "absent":
            gcp_v2.debug(module, action="delete")
            try:
                # --------- BEGIN delete code ---------
                delete_link: str = ""  # give it a chance for pre-delete to overload
                if delete_link == "":
                    delete_link = resource.build_link("delete")
                delete_retries = op_configs.delete.timeout
                delete_func = getattr(resource, op_configs.delete.verb)
                delete_async_uri = op_configs.delete.async_uri
                delete_async_func = getattr(resource, op_configs.delete.verb + "_async")
                gcp_v2.debug(
                    module,
                    msg="Destroying resource",
                    delete_link=delete_link,
                    async_uri=delete_async_uri,
                )

                if delete_async_uri != "":
                    new_obj = delete_async_func(delete_link, async_uri=delete_async_uri, retries=delete_retries)
                else:
                    new_obj = delete_func(delete_link)
                new_obj = resource.from_response(new_obj)
                gcp_v2.debug(module, new=new_obj, action="delete", post=False)
                gcp_v2.debug(module, new=new_obj, action="delete", post=True)
                # --------- END delete code ---------
            except Exception as e:
                module.fail_json(msg=str(e))

            changed = True
        else:
            if is_different:
                gcp_v2.debug(module, action="update")
                try:
                    # --------- BEGIN update code ---------
                    update_link: str = ""  # give it a chance for pre-update to overload
                    if update_link == "":
                        update_link = resource.build_link("update")
                    update_retries = op_configs.update.timeout
                    update_func = getattr(resource, op_configs.update.verb)
                    update_async_uri = op_configs.update.async_uri
                    update_async_func = getattr(resource, op_configs.update.verb + "_async")
                    gcp_v2.debug(
                        module,
                        msg="Updating resource",
                        update_link=update_link,
                        async_uri=update_async_uri,
                    )
                    if update_async_uri != "":
                        new_obj = update_async_func(update_link, async_uri=update_async_uri, retries=update_retries)
                    else:
                        new_obj = update_func(update_link)
                    new_obj = resource.with_kind(resource.from_response(new_obj))
                    gcp_v2.debug(module, new=new_obj, action="update", post=False)
                    gcp_v2.debug(module, new=new_obj, action="update", post=True)
                    # --------- END update code ---------
                except Exception as e:
                    module.fail_json(msg=str(e))

                changed = True
            else:
                new_obj = existing_obj

    new_obj = gcp_v2.filter_reserved_keys(new_obj)
    new_obj.update({"changed": changed})
    gcp_v2.debug(module, final_obj=new_obj, changed=changed)
    module.exit_json(**new_obj)


if __name__ == "__main__":
    main()
