#!/usr/bin/python
# -*- coding: utf-8 -*-
#
# Copyright (C) 2017-2026 Google
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
#

from __future__ import absolute_import, division, print_function

__metaclass__ = type

################################################################################
# Documentation
################################################################################

ANSIBLE_METADATA = {
    "metadata_version": "1.1",
    "status": ["preview"],
    "supported_by": "community",
}

DOCUMENTATION = r"""
---
author:
  - Google Inc. (@googlecloudplatform)
description:
  - '''A runtime is a Google-provisioned virtual machine (VM) that can run the code in your notebook (IPYNB file).'''
extends_documentation_fragment:
  - google.cloud.gcp
module: gcp_colab_runtime_info
notes:
  - 'API Reference: U(https://cloud.google.com/vertex-ai/docs/reference/rest/v1/projects.locations.notebookRuntimes)'
  - 'Create a runtime Guide: U(https://cloud.google.com/colab/docs/create-runtime)'
options:
  filters:
    description:
      - A list of filter expression strings used to filter the resources returned by the API.
      - Each string is a filter expression (e.g. C(some_field = "SOME_VALUE")).
      - Multiple expressions are combined with a logical AND.
      - Refer to the filter topic documentation U(https://cloud.google.com/sdk/gcloud/reference/topic/filters).
      - Refer to the IAP-160 filter syntax documentation U(https://google.aip.dev/160).
    elements: str
    type: list
  location:
    description:
      - 'The location for the resource: https://cloud.google.com/colab/docs/locations.'
    required: true
    type: str
requirements:
  - python >= 3.8
  - requests >= 2.18.4
  - google-auth >= 2.25.1
short_description: List GCP colab.Runtime resources
"""  # noqa: E501

EXAMPLES = r"""
- name: List all gcp_colab_runtime_info resources
  google.cloud.gcp_colab_runtime_info:
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result

- name: List gcp_colab_runtime_info resources matching given filters
  google.cloud.gcp_colab_runtime_info:
    filters:
      - display_name = "my-resource"
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result"""  # noqa: E501

RETURN = r"""
changed:
  description: Whether any changes were made (always false for info modules).
  returned: always
  type: bool
resources:
  contains:
    description:
      description:
        - The description of the Runtime.
      returned: when set
      type: str
    displayName:
      description:
        - The display name of the Runtime.
      returned: always
      type: str
    expirationTime:
      description:
        - Output only.
        - Timestamp when this NotebookRuntime will be expired.
      returned: success
      type: str
    isUpgradable:
      description:
        - Output only.
        - Checks if the NotebookRuntime is upgradable.
      returned: success
      type: bool
    notebookRuntimeTemplateRef:
      contains:
        notebookRuntimeTemplate:
          description:
            - The resource name of the NotebookRuntimeTemplate based on which a NotebookRuntime will be created.
          returned: always
          type: str
      description:
        - '''Runtime specific information used for NotebookRuntime creation.''.'
      returned: when set
      type: dict
    notebookRuntimeType:
      description:
        - Output only.
        - The type of the notebook runtime.
      returned: success
      type: str
    runtimeUser:
      description:
        - The user email of the NotebookRuntime.
      returned: always
      type: str
    state:
      description:
        - Output only.
        - The state of the runtime.
      returned: success
      type: str
  description: List of Colab.Runtime resources matching the supplied filters. May be empty, contain a single resource, or multiple resources.
  elements: dict
  returned: always
  type: list
"""  # noqa: E501

################################################################################
# Imports
################################################################################

from ansible_collections.google.cloud.plugins.module_utils import gcp_v2


################################################################################
# Main
################################################################################


def main():
    """Main function"""

    module = gcp_v2.Module(
        argument_spec=dict(
            filters=dict(
                type="list",
                elements="str",
            ),
            location=dict(
                type="str",
                required=True,
            ),
        ),
        supports_check_mode=True,
    )

    if not module.params["scopes"]:
        module.params["scopes"] = ["https://www.googleapis.com/auth/cloud-platform"]

    # info modules only require the "collection" url
    op_configs = gcp_v2.ResourceOpConfigs(
        base_url="https://{location}-aiplatform.googleapis.com/v1/",
        base_uri="projects/{project}/locations/{location}/notebookRuntimes",
        configs={},
    )

    info = gcp_v2.Resource(
        module.params,  # only need it to build the link
        module=module,
        product="Colab",
        kind="colab#runtime",
        op_configs=op_configs,
    )

    filter_exprs = module.params.get("filters") or []

    link = info.build_link("list")
    resources = info.list(link, key="notebookRuntimes", filters=filter_exprs)

    module.exit_json(changed=False, resources=resources)


if __name__ == "__main__":
    main()
