#!/usr/bin/python
# -*- coding: utf-8 -*-
#
# Copyright (C) 2017-2026 Google
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
#

from __future__ import absolute_import, division, print_function

__metaclass__ = type

################################################################################
# Documentation
################################################################################

ANSIBLE_METADATA = {
    "metadata_version": "1.1",
    "status": ["preview"],
    "supported_by": "community",
}

DOCUMENTATION = r"""
---
author:
  - Google Inc. (@googlecloudplatform)
description:
  - A Container Analysis note is a high-level piece of metadata that
  - describes a type of analysis that can be done for a resource.
extends_documentation_fragment:
  - google.cloud.gcp
  - google.cloud.info
module: gcp_containeranalysis_note_info
notes:
  - 'API Reference: U(https://cloud.google.com/container-analysis/api/reference/rest/)'
  - 'Creating Attestations (Occurrences) Guide: U(https://cloud.google.com/binary-authorization/docs/making-attestations)'
  - 'Official Documentation Guide: U(https://cloud.google.com/container-analysis/)'
options:
  filters:
    elements: str
    type: list
requirements:
  - python >= 3.8
  - requests >= 2.18.4
  - google-auth >= 2.25.1
short_description: List ContainerAnalysis.Note resources
"""  # noqa: E501

EXAMPLES = r"""
- name: List all gcp_containeranalysis_note_info resources
  google.cloud.gcp_containeranalysis_note_info:
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result

- name: List gcp_containeranalysis_note_info resources matching given filters
  google.cloud.gcp_containeranalysis_note_info:
    filters:
      - display_name = "my-resource"
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result"""  # noqa: E501

RETURN = r"""
changed:
  description: Whether any changes were made (always false for info modules).
  returned: always
  type: bool
resources:
  contains:
    attestationAuthority:
      contains:
        hint:
          contains:
            humanReadableName:
              description:
                - The human readable name of this Attestation Authority, for example "qa".
              returned: always
              type: str
          description:
            - This submessage provides human-readable hints about the purpose of the AttestationAuthority.
            - Because the name of a Note acts as its resource reference, it is important to disambiguate the canonical name of the Note (which might be a UUID for security purposes) from "readable" names more suitable for debug output.
            - Note that these hints should NOT be used to look up AttestationAuthorities in security sensitive contexts, such as when looking up Attestations to verify.
          returned: always
          type: dict
      description:
        - Note kind that represents a logical attestation "role" or "authority".
        - For example, an organization might have one AttestationAuthority for "QA" and one for "build".
        - This Note is intended to act strictly as a grouping mechanism for the attached Occurrences (Attestations).
        - This grouping mechanism also provides a security boundary, since IAM ACLs gate the ability for a principle to attach an Occurrence to a given Note.
        - It also provides a single point of lookup to find all attached Attestation Occurrences, even if they don't all live in the same project.
      returned: always
      type: dict
    createTime:
      description:
        - The time this note was created.
      returned: success
      type: str
    expirationTime:
      description:
        - Time of expiration for this note.
        - Leave empty if note does not expire.
      returned: when set
      type: str
    kind:
      description:
        - The type of analysis this note describes.
      returned: success
      type: str
    longDescription:
      description:
        - A detailed description of the note.
      returned: when set
      type: str
    name:
      description:
        - The name of the note.
      returned: always
      type: str
    relatedNoteNames:
      description:
        - Names of other notes related to this note.
      elements: str
      returned: when set
      type: list
    relatedUrl:
      contains:
        label:
          description:
            - Label to describe usage of the URL.
          returned: when set
          type: str
        url:
          description:
            - Specific URL associated with the resource.
          returned: always
          type: str
      description:
        - URLs associated with this note and related metadata.
      elements: dict
      returned: when set
      type: list
    shortDescription:
      description:
        - A one sentence description of the note.
      returned: when set
      type: str
    updateTime:
      description:
        - The time this note was last updated.
      returned: success
      type: str
  description: List of ContainerAnalysis.Note resources matching the supplied filters. May be empty, contain a single resource, or multiple resources.
  elements: dict
  returned: always
  type: list
"""  # noqa: E501

################################################################################
# Imports
################################################################################

from ansible_collections.google.cloud.plugins.module_utils import gcp_v2

################################################################################
# Main
################################################################################


def main():
    """Main function"""

    module = gcp_v2.Module(
        argument_spec=dict(
            filters=dict(
                type="list",
                elements="str",
            ),
        ),
        supports_check_mode=True,
    )

    if not module.params["scopes"]:
        module.params["scopes"] = ["https://www.googleapis.com/auth/cloud-platform"]

    # info modules only require the "collection" url
    op_configs = gcp_v2.ResourceOpConfigs(
        base_url="https://containeranalysis.googleapis.com/v1/", base_uri="projects/{project}/notes", configs={}
    )

    info = gcp_v2.Resource(
        module.params,  # only need it to build the link
        module=module,
        product="ContainerAnalysis",
        kind="containeranalysis#note",
        op_configs=op_configs,
    )

    filter_exprs = module.params.get("filters") or []

    link = info.build_link("list")
    resources = info.list(link, key="notes", filters=filter_exprs)

    module.exit_json(changed=False, resources=gcp_v2.filter_reserved_keys(resources))


if __name__ == "__main__":
    main()
