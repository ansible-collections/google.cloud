#!/usr/bin/python
# -*- coding: utf-8 -*-
#
# Copyright (C) 2017-2026 Google
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
#

from __future__ import absolute_import, division, print_function

__metaclass__ = type

################################################################################
# Documentation
################################################################################

ANSIBLE_METADATA = {
    "metadata_version": "1.1",
    "status": ["preview"],
    "supported_by": "community",
}

DOCUMENTATION = r"""
---
author:
  - Google Inc. (@googlecloudplatform)
description:
  - Vertex AI Feature Group Feature is feature metadata information.
extends_documentation_fragment:
  - google.cloud.gcp
module: gcp_vertexai_feature_group_feature_info
notes:
  - 'API Reference: U(https://cloud.google.com/vertex-ai/docs/reference/rest/v1beta1/projects.locations.featureGroups.features)'
  - 'Creating a Feature Guide: U(https://cloud.google.com/vertex-ai/docs/featurestore/latest/create-feature)'
options:
  feature_group:
    description:
      - The name of the Feature Group.
    required: true
    type: str
  filters:
    description:
      - A list of filter expression strings used to filter the resources returned by the API.
      - Each string is a filter expression (e.g. C(some_field = "SOME_VALUE")).
      - Multiple expressions are combined with a logical AND.
      - Refer to the filter topic documentation U(https://cloud.google.com/sdk/gcloud/reference/topic/filters).
      - Refer to the IAP-160 filter syntax documentation U(https://google.aip.dev/160).
    elements: str
    type: list
  region:
    description:
      - The region for the resource.
      - It should be the same as the feature group's region.
    required: true
    type: str
requirements:
  - python >= 3.8
  - requests >= 2.18.4
  - google-auth >= 2.25.1
short_description: List GCP vertexai.FeatureGroupFeature resources
"""  # noqa: E501

EXAMPLES = r"""
- name: List all gcp_vertexai_feature_group_feature_info resources
  google.cloud.gcp_vertexai_feature_group_feature_info:
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result

- name: List gcp_vertexai_feature_group_feature_info resources matching given filters
  google.cloud.gcp_vertexai_feature_group_feature_info:
    filters:
      - display_name = "my-resource"
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result"""  # noqa: E501

RETURN = r"""
changed:
  description: Whether any changes were made (always false for info modules).
  returned: always
  type: bool
resources:
  contains:
    createTime:
      description:
        - The timestamp of when the FeatureGroup was created in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits.
      returned: success
      type: str
    description:
      description:
        - The description of the FeatureGroup.
      returned: when set
      type: str
    labels:
      description:
        - The labels with user-defined metadata to organize your FeatureGroup.
        - '**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.'
      returned: when set
      type: dict
    updateTime:
      description:
        - The timestamp of when the FeatureGroup was last updated in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits.
      returned: success
      type: str
    versionColumnName:
      description:
        - The name of the BigQuery Table/View column hosting data for this version.
        - If no value is provided, will use featureId.
      returned: when set
      type: str
  description: List of VertexAI.FeatureGroupFeature resources matching the supplied filters. May be empty, contain a single resource, or multiple resources.
  elements: dict
  returned: always
  type: list
"""  # noqa: E501

################################################################################
# Imports
################################################################################

from ansible_collections.google.cloud.plugins.module_utils import gcp_v2


################################################################################
# Main
################################################################################


def main():
    """Main function"""

    module = gcp_v2.Module(
        argument_spec=dict(
            feature_group=dict(
                type="str",
                required=True,
            ),
            filters=dict(
                type="list",
                elements="str",
            ),
            region=dict(
                type="str",
                required=True,
            ),
        ),
        supports_check_mode=True,
    )

    if not module.params["scopes"]:
        module.params["scopes"] = ["https://www.googleapis.com/auth/cloud-platform"]

    # info modules only require the "collection" url
    op_configs = gcp_v2.ResourceOpConfigs(
        base_url="https://{region}-aiplatform.googleapis.com/v1/",
        base_uri="projects/{project}/locations/{region}/featureGroups/{feature_group}/features",
        configs={},
    )

    info = gcp_v2.Resource(
        module.params,  # only need it to build the link
        module=module,
        product="VertexAI",
        kind="vertexai#featureGroupFeature",
        op_configs=op_configs,
    )

    filter_exprs = module.params.get("filters") or []

    link = info.build_link("list")
    resources = info.list(link, key="features", filters=filter_exprs)

    module.exit_json(changed=False, resources=resources)


if __name__ == "__main__":
    main()
