#!/usr/bin/python
# -*- coding: utf-8 -*-
#
# Copyright (C) 2017-2026 Google
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
# ----------------------------------------------------------------------------
#

from __future__ import absolute_import, division, print_function

__metaclass__ = type

################################################################################
# Documentation
################################################################################

ANSIBLE_METADATA = {
    "metadata_version": "1.1",
    "status": ["preview"],
    "supported_by": "community",
}

DOCUMENTATION = r"""
---
author:
  - Google Inc. (@googlecloudplatform)
description:
  - A policy for container image binary authorization.
extends_documentation_fragment:
  - google.cloud.gcp
  - google.cloud.info
module: gcp_binaryauthorization_policy_info
notes:
  - 'API Reference: U(https://cloud.google.com/binary-authorization/docs/reference/rest/)'
  - 'Official Documentation Guide: U(https://cloud.google.com/binary-authorization/)'
options:
  filters:
    elements: str
    type: list
requirements:
  - python >= 3.8
  - requests >= 2.18.4
  - google-auth >= 2.25.1
short_description: List BinaryAuthorization.Policy resources
"""  # noqa: E501

EXAMPLES = r"""
- name: List all gcp_binaryauthorization_policy_info resources
  google.cloud.gcp_binaryauthorization_policy_info:
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result

- name: List gcp_binaryauthorization_policy_info resources matching given filters
  google.cloud.gcp_binaryauthorization_policy_info:
    filters:
      - display_name = "my-resource"
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_cred_kind }}"
    service_account_file: "{{ gcp_cred_file }}"
  register: result"""  # noqa: E501

RETURN = r"""
changed:
  description: Whether any changes were made (always false for info modules).
  returned: always
  type: bool
resources:
  contains:
    admissionWhitelistPatterns:
      contains:
        namePattern:
          description:
            - An image name pattern to whitelist, in the form `registry/path/to/image`.
            - This supports a trailing * as a wildcard, but this is allowed only in text after the registry/ part.
          returned: always
          type: str
      description:
        - A whitelist of image patterns to exclude from admission rules.
        - If an image's name matches a whitelist pattern, the image's admission requests will always be permitted regardless of your admission rules.
      elements: dict
      returned: when set
      type: list
    clusterAdmissionRules:
      contains:
        cluster:
          description:
            - The identifier for this ClusterAdmissionRules entry, used as the map key.
          returned: success
          type: str
        enforcementMode:
          description:
            - The action when a pod creation is denied by the admission rule.
          returned: always
          type: str
        evaluationMode:
          description:
            - How this admission rule will be evaluated.
          returned: always
          type: str
        requireAttestationsBy:
          description:
            - The resource names of the attestors that must attest to a container image.
            - If the attestor is in a different project from the policy, it should be specified in the format `projects/*/attestors/*`.
            - Each attestor must exist before a policy can reference it.
            - To add an attestor to a policy the principal issuing the policy change request must be able to read the attestor resource.
            - 'Note: this field must be non-empty when the evaluation_mode field specifies REQUIRE_ATTESTATION, otherwise it must be empty.'
          elements: str
          returned: when set
          type: list
      description:
        - Per-cluster admission rules.
        - An admission rule specifies either that all container images used in a pod creation request must be attested to by one or more attestors, that all pod creations will be allowed, or that all pod creations will be denied.
        - There can be at most one admission rule per cluster spec.
        - 'Identifier format: `{{location}}.{{clusterId}}`.'
        - A location is either a compute zone (e.g.
        - '`us-central1-a`) or a region (e.g.'
        - '`us-central1`).'
      elements: dict
      returned: when set
      type: list
    defaultAdmissionRule:
      contains:
        enforcementMode:
          description:
            - The action when a pod creation is denied by the admission rule.
          returned: always
          type: str
        evaluationMode:
          description:
            - How this admission rule will be evaluated.
          returned: always
          type: str
        requireAttestationsBy:
          description:
            - The resource names of the attestors that must attest to a container image.
            - If the attestor is in a different project from the policy, it should be specified in the format `projects/*/attestors/*`.
            - Each attestor must exist before a policy can reference it.
            - To add an attestor to a policy the principal issuing the policy change request must be able to read the attestor resource.
            - 'Note: this field must be non-empty when the evaluation_mode field specifies REQUIRE_ATTESTATION, otherwise it must be empty.'
          elements: str
          returned: when set
          type: list
      description:
        - Default admission rule for a cluster without a per-cluster admission rule.
      returned: always
      type: dict
    description:
      description:
        - A descriptive comment.
      returned: when set
      type: str
    globalPolicyEvaluationMode:
      description:
        - Controls the evaluation of a Google-maintained global admission policy for common system-level images.
        - Images not covered by the global policy will be subject to the project admission policy.
      returned: when set
      type: str
  description: List of BinaryAuthorization.Policy resources matching the supplied filters. May be empty, contain a single resource, or multiple resources.
  elements: dict
  returned: always
  type: list
"""  # noqa: E501

################################################################################
# Imports
################################################################################

from ansible_collections.google.cloud.plugins.module_utils import gcp_v2

################################################################################
# Main
################################################################################


def main():
    """Main function"""

    module = gcp_v2.Module(
        argument_spec=dict(
            filters=dict(
                type="list",
                elements="str",
            ),
        ),
        supports_check_mode=True,
    )

    if not module.params["scopes"]:
        module.params["scopes"] = ["https://www.googleapis.com/auth/cloud-platform"]

    # info modules only require the "collection" url
    op_configs = gcp_v2.ResourceOpConfigs(
        base_url="https://binaryauthorization.googleapis.com/v1/", base_uri="projects/{project}/policy", configs={}
    )

    info = gcp_v2.Resource(
        module.params,  # only need it to build the link
        module=module,
        product="BinaryAuthorization",
        kind="binaryauthorization#policy",
        op_configs=op_configs,
    )

    filter_exprs = module.params.get("filters") or []

    link = info.build_link("list")
    resources = info.list(link, key="clusterAdmissionRules", filters=filter_exprs)

    module.exit_json(changed=False, resources=gcp_v2.filter_reserved_keys(resources))


if __name__ == "__main__":
    main()
